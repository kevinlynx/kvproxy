* group
    * primary group (get & set)
    * slave group (set)
* servers locator
    * local conf
    * custom (e.g load from nameserver)
* key locator
    * consistent hash
* service abstract
    * memcache implementation
* replica in cluster
* local cache
